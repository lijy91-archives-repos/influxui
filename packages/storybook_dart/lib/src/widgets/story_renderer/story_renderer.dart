import 'package:flutter/widgets.dart';
import 'package:storybook_dart/src/story_obj.dart';

class StoryRenderer extends StatelessWidget {
  const StoryRenderer({
    Key? key,
    required this.story,
  }) : super(key: key);

  final StoryObj story;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
