import 'package:flutter/widgets.dart';

import 'package:rise_ui/src/widgets/loader/loader_theme.dart';

const _defaults = LoaderThemeData(
  tinySize: Size.square(18),
  smallSize: Size.square(22),
  mediumSize: Size.square(36),
  largeSize: Size.square(44),
  bigSize: Size.square(58),
);

final kDarkLoaderThemeDefaults = _defaults;

final kLightLoaderThemeDefaults = _defaults;
